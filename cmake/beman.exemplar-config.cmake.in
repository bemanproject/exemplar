# SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception

include(CMakeFindDependencyMacro)
@BEMAN_FIND_DEPENDENCIES@

# Semicolon separated list of the project's dependencies
# i.e.: "fmt 12.1.0 EXACT;cxxopts 2.2.0"
# string(REGEX MATCHALL "[^;]+" SEPARATE_DEPENDENCIES "@BEMAN_DEPENDENCIES@" )
# foreach(dependency ${SEPARATE_DEPENDENCIES})
#   string(REPLACE " " ";" args "${dependency}")
#   find_dependency(${args})
# endforeach()

# FIXME: what is this for? set(BEMAN_EXEMPLAR_VERSION @PROJECT_VERSION@)

@PACKAGE_INIT@

include(${CMAKE_CURRENT_LIST_DIR}/@PROJECT_NAME@-targets.cmake)

check_required_components(@PROJECT_NAME@)
